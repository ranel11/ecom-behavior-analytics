# 🛒 eCommerce Clickstream Analytics System (SQLite & SQL)

## 📌 Описание проекта
Проект посвящен построению продуктовой аналитики на основе лога кликстрима (clickstream data) крупного интернет-магазина за ноябрь 2019 года. 

Основная цель проекта — преобразовать сырые неотсортированные данные объемного лога в оптимизированную реляционную базу данных SQLite, спроектировать витрины данных (Data Marts) и провести продуктовый анализ (воронки конверсий, Retention, LTV и поведение пользователей).

---

## 🛠 Технологический стек
* **Язык:** Python 3.x
* **СУБД:** SQLite3
* **Библиотеки:** Pandas, time, sqlite3
* **Оптимизация:** B-Tree индексирование, пакетная загрузка (Chunking)

---

## 📊 Описание сырой таблицы (`events`)
Сырые данные представляют собой файл CSV объемом **67+ миллионов строк**, содержащий информацию о действиях пользователей на сайте.

### Структура колонок:
| Колонка | Тип данных | Описание |
| :--- | :--- | :--- |
| `event_time` | DATETIME | Дата и время события (UTC) |
| `event_type` | TEXT | Тип действия: `view` (просмотр), `cart` (в корзину), `purchase` (покупка) |
| `product_id` | INT | Уникальный ID товара |
| `category_id` | BIGINT | Уникальный ID категории |
| `category_code` | TEXT | Текстовая категория (например, `electronics.smartphone`) |
| `brand` | TEXT | Бренд товара |
| `price` | REAL | Цена товара в $ |
| `user_id` | INT | Уникальный ID пользователя |
| `user_session` | TEXT | Уникальный UUID сессии |

---

## 🏗 Архитектура данных
```text
[ Сырой CSV-файл (8 GB) ]
         │
         ▼ (Chunking via Pandas)
[ SQLite: events (67M+ строк) ]
         │
         ▼ (B-Tree Индексы: user_id, user_session, event_type, event_time)
 ┌───────┼────────────────┐
 │       │                │
 ▼       ▼                ▼
[ dim_sessions ]   [ dim_users ]   [ dim_products ]
 (Воронка/Сессии)   (LTV/Профили)   (Справочник)
```
---

## 🏛 Структура витрин данных (Data Marts Layer)

### 1. Витрина сессий (`dim_sessions`)
Агрегированная таблица уровня отдельных пользовательских сессий. Служит основой для расчета сессионных воронок конверсии и оценки длительности визитов.

| Колонка | Тип данных | Описание |
| :--- | :--- | :--- |
| `user_session` | TEXT | Уникальный UUID сессии |
| `user_id` | INT | Уникальный ID пользователя |
| `start_session` | DATETIME | Время начала сессии (первое событие) |
| `end_session` | DATETIME | Время окончания сессии (последнее событие) |
| `session_duration_sec` | INT | Длительность сессии в секундах |
| `views_count` | INT | Количество просмотров товаров в сессии |
| `cart_count` | INT | Количество добавлений в корзину в сессии |
| `purchase_count` | INT | Количество совершенных покупок в сессии |
| `has_view` | INT (0/1) | Флаг наличия хотя бы одного просмотра в сессии |
| `has_cart` | INT (0/1) | Флаг наличия хотя бы одного добавления в корзину |
| `has_purchase` | INT (0/1) | Флаг наличия хотя бы одной покупки в сессии |
| `total_revenue` | REAL | Суммарная выручка, сгенерированная за сессию ($) |

### 2. Витрина пользователей (`dim_users`)
Агрегированная витрина клиентского профиля. Позволяет оценивать жизненный цикл пользователя (Lifetime), показатели вовлеченности, RFM-сегментацию и LTV.

| Колонка | Тип данных | Описание |
| :--- | :--- | :--- |
| `user_id` | INT | Уникальный ID пользователя |
| `lifetime_days` | INT | Время между первой и последней сессией пользователя (в днях) |
| `recency_days` | INT | Количество дней с последней активности до конца периода базы |
| `total_sessions` | INT | Общее количество сессий пользователя |
| `total_views` | INT | Суммарное количество просмотров товаров |
| `total_cart` | INT | Суммарное количество добавлений в корзину |
| `total_purchases` | INT | Суммарное количество совершенных покупок |
| `ltv_revenue` | REAL | Накопленная выручка с пользователя (LTV) |
| `is_buyer` | INT (0/1) | Бинарный маркер платящего пользователя |

### 3. Товарная витрина (`dim_products`)
Агрегированная справочная витрина по всем товарам каталога. Используется для ABC-анализа, анализа воронки конверсии конкретных товаров и ценового позиционирования.

| Колонка | Тип данных | Описание |
| :--- | :--- | :--- |
| `product_id` | INT | Уникальный ID товара |
| `category_id` | BIGINT | Идентификатор категории товара |
| `brand` | TEXT | Бренд товара (с обработкой пропусков `Unknown`) |
| `unique_users` | INT | Количество уникальных пользователей, взаимодействовавших с товаром |
| `views_product` | INT | Общее количество просмотров товара |
| `cart_product` | INT | Общее количество добавлений товара в корзину |
| `purchase_product` | INT | Общее количество покупок товара |
| `avg_price` | REAL | Средняя фактическая цена продажи товара ($) |
| `min_price` | REAL | Минимальная зафиксированная цена товара ($) |
| `max_price` | REAL | Максимальная зафиксированная цена товара ($) |
| `product_revenue` | REAL | Суммарная выручка, принесенная товаром ($) |

---

## 📝 Дорожная карта проекта (Roadmap / Tasks)

✅ **Этап 1: ETL и Оптимизация БД**
* ✅ Настройка пакетного импорта CSV-файла (67M+ строк) в SQLite через Pandas chunks.
* ✅ Создание структуры таблицы `events` с валидацией типов данных.
* ✅ Проектирование и генерация B-Tree индексов (`idx_events_user_id`, `idx_events_user_session`, `idx_events_type`, `idx_events_time`).

✅ **Этап 2: Проектирование витрин данных (Data Marts)**
* ✅ Сборка витрины `dim_sessions` (агрегация по сессиям, длительность, флаги воронок).
* ✅ Сборка витрины `dim_users` (профили пользователей, lifetime, recency_days, LTV, вовлеченность).
* ✅ Сборка витрины `dim_products` (справочник товаров, очистка брендов, диапазоны цен, выручка).

✅ **Этап 3: Продуктовая аналитика и SQL-исследования**
* ✅ **Блок 1: Исследование продуктовой воронки и сегментация пользователей.**
  * ✅ Расчет общей воронки (View → Cart → Purchase) и поиск точек отвала.
  * ✅ Сегментация по вовлеченности (`total_sessions = 1` vs `total_sessions > 1`).
  * ✅ Исследование цен и ограничений агрегированных справочников (`dim_products`).
* ⏳ **Блок 2: Анализ времени принятия решения о покупке (Time-to-Purchase).**
* ⏳ **Блок 3: Когортный анализ и оценка возвращаемости (Retention Rate).**
* ⏳ **Блок 4: Анализ брендов и категорий (Матрица популярности vs конверсии).**

⏳ **Этап 4: Визуализация и Выводы**
* 🟩 Построение графиков воронок и когорт.
* 🟩 Формулирование итоговых продуктовых гипотез и рекомендаций для бизнеса.

---

## 📈 Результаты исследований (Product Analytics Insights)

### 📊 Блок 1. Продуктовая воронка и сегментация аудитории

В рамках первого блока аналитики была изучена конверсия пользователей на разных этапах воронки и проведена сегментация аудитории по уровню вовлеченности (`total_sessions`).

#### 1. Сравнительный анализ: Односессионные vs Возвращающиеся пользователи
Было проведено разделение аудитории на клиентов с 1 сессией (`new_cust`) и клиентов с 2+ сессиями (`othen_cust`). 

| Метрика | Односессионные (`total_sessions = 1`) | Возвращающиеся (`total_sessions > 1`) | Прирост / Эффект |
| :--- | :--- | :--- | :--- |
| **Доля от общей базы** | **44%** | **55%** | База распределена практически поровну |
| **Конверсия в покупку ($CR$)** | **3%** | **18%** | **Рост в 6 раз!** 🚀 |
| **Доля добавления в корзину** | **7%** | **34%** | **Рост почти в 5 раз** |
| **$CR_{Cart \rightarrow Purchase}$** | **50%** | **53%** | Сопоставимая конверсия на этапе чекаута |
| **Средний LTV ($)** | **$13** | **$125** | **Рост в 9.6 раз!** 💰 |
| **Среднее число просмотров** | **3** | **28** | Вовлеченность выше в 9.3 раза |

**Ключевые продуктовые выводы:**
1. **Вовлеченность — главный драйвер продаж:** Пользователь с 1 сессией имеет крайне низкий $CR$ (**3%**). Повторный визит увеличивает вероятность покупки до **18%**. В сервисе практически отсутствуют «импульсивные» покупки с первого взгляда — пользователям требуется время на знакомство с каталогом.
2. **Точка отвала новичков — этап ДО корзины:** Лишь **7%** односессионных пользователей добавляют товар в корзину (против **34%** у возвращающихся). При этом, если новичок положит товар в корзину, он покупает с высокой вероятность (**50%**). Проблема первой сессии — не в оформлении заказа, а в слабой мотивации и неспособности каталога «зацепить» пользователя на старте.
3. **Экономика удержания:** Возвращающийся клиент генерирует в среднем **$125 LTV** против **$13 LTV** у одноразового. Фокус маркетинга должен быть смещен с закупки холодного трафика на Retention-механики (Push, Retargeting, Email-цепочки).

#### 2. Методологические особенности работы с `dim_products`
При попытке анализа ценовых сегментов напрямую через справочник `dim_products` были выявлены следующие продуктовые и технические особенности данных:
* **Агрегация за весь жизненный цикл:** Поля `views_product` и `purchase_product` в `dim_products` показывают накопленные факты за все время. Конверсия на уровне единицы товара некорректно отражает поведение конкретного пользователя (например, покупки «в 1 клик» дают $CR > 100\%$).
* **Обработка пропусков:** При расчете перцентилей цен (`NTILE`) необходимо явно фильтровать `WHERE avg_price IS NOT NULL`, чтобы неразмеченные товары не искажали сегментацию.